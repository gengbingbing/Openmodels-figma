
  # Openmodels

  This is a code bundle for Openmodels. The original project is available at https://www.figma.com/design/UkMBIW9y9Ph8dRK8pb5gTR/Openmodels.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  